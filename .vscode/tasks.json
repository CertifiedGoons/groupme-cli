{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build and run",
            "type": "shell",
            "command": "clear && mkdir -p ${workspaceFolder}/build && gcc -lncurses ${workspaceFolder}/src/main.cpp -o ${workspaceFolder}/build/${workspaceFolderBasename} && clear && ${workspaceFolder}/build/${workspaceFolderBasename}",
            "args": [],
            "group": "build",
            "presentation": {
                "reveal": "always"
            },
            "problemMatcher": []
        }
    ]
}